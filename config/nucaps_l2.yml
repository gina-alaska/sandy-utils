driver: viirs2awips.sh
driver_crefl: crefl2awips.sh
configs:
  npp:
   driver: "run_heap.bash -s npp --noclean -d /opt/cspp/dyno/nucaps_anc -vv -m HR_IRMW -p 2 "
   save: 
     - "*.nc"
  noaa20:
    driver: "run_heap.bash -s j01 --noclean -d /opt/cspp/dyno/nucaps_anc -vv  -m HR_IRMW -p 2 "
    save:
      - "*.nc"
  metop-b:
    driver: "run_heap.bash -s m01 --noclean -d /opt/cspp/dyno/nucaps_anc -vv -p 2 "
    save: 
      - "*.nc"
  metop-c:
    driver: "run_heap.bash -s m03 --noclean -d /opt/cspp/dyno/nucaps_anc -vv -p 2 "
    save:
      - "*.nc"     
limits:
  processor: 2
